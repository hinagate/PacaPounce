__pycache__/
*.pyc
.pytest_cache/
.env
# Runtime audit/state stays out of the repository by default.
data/*.jsonl
data/*.log
data/risk_state.json
data/mcp_calls.json
data/accounts/
# ...except the decision record the dashboard cites as its audit trail.
# The page states that presentation is bounded because the full append-only
# record exists; a link to a file nobody can open would not be evidence.
!data/accounts/PA3ZX2FIASSZ/
!data/accounts/PA3ZX2FIASSZ/verdicts.jsonl
# run.py --loop writes the live 60-second snapshot here; the tracked
# dashboard/index.html is the published copy built with --published.
dashboard/runtime/
.venv/
